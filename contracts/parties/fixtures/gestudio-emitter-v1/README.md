# Gestudio emitter v1 fixtures

`page-001.json` and `page-002.json` are exact UTF-8 response bodies generated by
the Gestudio emitter at commit
`4c88635e76d7814b91e1a8baacf7a9db3a8ca81d`. They contain synthetic references
only. Signatures and runtime secrets are intentionally not versioned.

The cross-repository smoke regenerates equivalent runtime artifacts and verifies
their exact HMAC before Jere Platform imports them.
